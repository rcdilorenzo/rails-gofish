describe "Drawing", ->
  beforeEach ->
    @drawing = new Drawing(null)

  # it "should move a subfigure when we click and drag", ->
    # rect = @drawing.rect1
    # centerX = rect.x
    # centerY = rect.y
    # @drawing.mouseDown(new Point(centerX, centerY))
    # @drawing.mouseMove(new Point(rect.x + 10, rect.y + 5))
    # expect(rect.x).toBe(centerX + 10)
    # expect(rect.y).toBe(centerY + 5)

